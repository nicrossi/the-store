apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ui
  namespace: the-store
  annotations:
    konghq.com/plugins: rate-limit-ui
spec:
  parentRefs:
    - name: kong
      namespace: kong
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ui
          namespace: the-store
          kind: Service
          port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ui-orders
  namespace: the-store
  annotations:
    konghq.com/plugins: rate-limit-ui,key-auth
spec:
  parentRefs:
    - name: kong
      namespace: kong
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /checkout
        - path:
            type: PathPrefix
            value: /cart
      backendRefs:
        - name: ui
          namespace: the-store
          kind: Service
          port: 80