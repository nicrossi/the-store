apiVersion: kuma.io/v1alpha1
kind: MeshTimeout
metadata:
  name: global-timeouts
spec:
  targetRef:
    kind: Mesh
  to:
    - targetRef:
        kind: MeshService
        name: "*"
      default:
        http:
          requestTimeout: 5s